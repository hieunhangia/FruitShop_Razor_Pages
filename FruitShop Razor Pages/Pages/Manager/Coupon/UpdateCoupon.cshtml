@page "{id:int}"
@using Repository.Constants
@model FruitShop_Razor_Pages.Pages.Manager.Coupon.UpdateCouponModel

@{
    ViewData["Title"] = "Chi tiết mã giảm giá";
}

<div class="container py-5">

    @if (TempData["StatusMessage"] is string statusMessage)
    {
        <div class="alert alert-success rounded-4">@statusMessage</div>
    }
    
    @if (TempData["ErrorMessage"] is string errorMessage)
    {
        <div class="alert alert-danger rounded-4">@errorMessage</div>
    }

    <div class="row g-4">
        <div class="col-lg-12">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body">
                    <h5 class="card-title fw-semibold">Cập nhật mã giảm giá</h5>
                    <form method="post" class="mt-3">
                        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>
                        <input type="hidden" asp-for="CouponUpdateDto.Id" />

                        <div class="mb-3">
                            <label asp-for="CouponUpdateDto.Description" class="form-label fw-medium">Mô tả</label>
                            <input asp-for="CouponUpdateDto.Description" class="form-control" />
                            <span asp-validation-for="CouponUpdateDto.Description" class="text-danger"></span>
                        </div>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <label asp-for="CouponUpdateDto.DiscountType" class="form-label fw-medium">Loại giảm giá</label>
                                <select asp-for="CouponUpdateDto.DiscountType" class="form-select">
                                    <option value="@DiscountType.Percentage">Phần trăm</option>
                                    <option value="@DiscountType.FixedAmount">Số tiền</option>
                                </select>
                                <span asp-validation-for="CouponUpdateDto.DiscountType" class="text-danger"></span>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="CouponUpdateDto.DiscountValue" class="form-label fw-medium">Giá trị giảm</label>
                                <input asp-for="CouponUpdateDto.DiscountValue" class="form-control" type="number" min="0" />
                                <span asp-validation-for="CouponUpdateDto.DiscountValue" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-6">
                                <label asp-for="CouponUpdateDto.MaxDiscountAmount" class="form-label fw-medium">Giảm tối đa</label>
                                <input asp-for="CouponUpdateDto.MaxDiscountAmount" class="form-control" type="number" min="0" />
                                <span asp-validation-for="CouponUpdateDto.MaxDiscountAmount" class="text-danger"></span>
                            </div>
                            <div class="col-md-6">
                                <label asp-for="CouponUpdateDto.MinOrderAmount" class="form-label fw-medium">Đơn tối thiểu</label>
                                <input asp-for="CouponUpdateDto.MinOrderAmount" class="form-control" type="number" min="0" />
                                <span asp-validation-for="CouponUpdateDto.MinOrderAmount" class="text-danger"></span>
                            </div>
                        </div>

                        <div class="row g-3 mt-1">
                            <div class="col-md-6">
                                <label asp-for="CouponUpdateDto.LoyaltyPointsCost" class="form-label fw-medium">Điểm đổi</label>
                                <input asp-for="CouponUpdateDto.LoyaltyPointsCost" class="form-control" type="number" min="0" />
                                <span asp-validation-for="CouponUpdateDto.LoyaltyPointsCost" class="text-danger"></span>
                            </div>
                            <div class="col-md-6 d-flex align-items-end">
                                <div class="form-check">
                                    <input asp-for="CouponUpdateDto.IsActive" class="form-check-input" />
                                    <label asp-for="CouponUpdateDto.IsActive" class="form-check-label">Trạng thái</label>
                                </div>
                            </div>
                        </div>

                        <div class="d-flex justify-content-between mt-4">
                            <a asp-page="./DisplayCoupons" class="btn btn-secondary">Trở lại</a>
                            <button type="submit" class="btn btn-success">Lưu thay đổi</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
