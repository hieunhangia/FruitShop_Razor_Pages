@page
@model FruitShop_Razor_Pages.Pages.SalesStaff.Product.ProductManagementModel
@{
    ViewData["Title"] = "Quản lý sản phẩm";
}

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold">Danh sách sản phẩm</h2>
        <a asp-page="./ProductCreate" class="btn btn-primary rounded-pill px-4">Thêm mới</a>
    </div>

    <form method="get" class="card shadow-sm border-0 mb-4">
        <div class="card-body row g-3">
            <div class="col-md-4">
                <input type="text" asp-for="RequestData.Filter.SearchName" class="form-control" placeholder="Tìm kiếm tên sản phẩm...">
            </div>
            <div class="col-md-3">
                <select asp-for="RequestData.Filter.IsActive" class="form-select">
                    <option value="">Tất cả trạng thái</option>
                    <option value="true">Đang hoạt động</option>
                    <option value="false">Đã khóa (Xóa mềm)</option>
                </select>
            </div>
            <div class="col-md-2">
                <button type="submit" class="btn btn-secondary w-100">Lọc</button>
            </div>
        </div>
    </form>

    <div class="table-responsive shadow-sm rounded">
        <table class="table table-hover align-middle mb-0 bg-white">
            <thead class="table-light">
                <tr>
                    <th>Hình ảnh</th>
                    <th>Tên sản phẩm</th>
                    <th>Giá</th>
                    <th>Tồn kho</th>
                    <th>Trạng thái</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Products?.Items.Count > 0)
                {
                    foreach (var item in Model.Products.Items)
                    {
                        <tr onclick="window.location.href='./ProductDetail/@item.Id'" style="cursor: pointer;">
                            <td><img src="@item.ImageFilePath" alt="@item.Name" class="rounded object-fit-cover" style="width: 50px; height: 50px;" /></td>
                            <td class="fw-bold">@item.Name</td>
                            <td>@item.Price.ToString("N0") đ / @item.ProductUnitName</td>
                            <td>@item.Quantity</td>
                            <td>
                                <span class="badge @(item.IsActive ? "bg-success" : "bg-danger")">
                                    @(item.IsActive ? "Hoạt động" : "Đã khóa")
                                </span>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>

    @if (Model.Products?.TotalPages > 1)
    {
        <nav aria-label="Page navigation" class="mt-4">
            <ul class="pagination justify-content-end mb-0">
                <li class="page-item @(Model.Products.HasPreviousPage ? "" : "disabled")">
                    <a class="page-link" asp-page="./ProductManagement"
                       asp-route-RequestData.PageIndex="@(Model.Products.PageIndex - 1)"
                       asp-route-RequestData.Filter.SearchName="@Model.RequestData.Filter.SearchName"
                       asp-route-RequestData.Filter.IsActive="@Model.RequestData.Filter.IsActive">Trước</a>
                </li>
                @for (var i = 1; i <= Model.Products.TotalPages; i++)
                {
                    <li class="page-item @(i == Model.Products.PageIndex ? "active" : "")">
                        <a class="page-link" asp-page="./ProductManagement"
                           asp-route-RequestData.PageIndex="@i"
                           asp-route-RequestData.Filter.SearchName="@Model.RequestData.Filter.SearchName"
                           asp-route-RequestData.Filter.IsActive="@Model.RequestData.Filter.IsActive">@i</a>
                    </li>
                }
                <li class="page-item @(Model.Products.HasNextPage ? "" : "disabled")">
                    <a class="page-link" asp-page="./ProductManagement"
                       asp-route-RequestData.PageIndex="@(Model.Products.PageIndex + 1)"
                       asp-route-RequestData.Filter.SearchName="@Model.RequestData.Filter.SearchName"
                       asp-route-RequestData.Filter.IsActive="@Model.RequestData.Filter.IsActive">Sau</a>
                </li>
            </ul>
        </nav>
    }
</div>